name: Context Examples

on: ['push']

jobs:
  my-context:
    runs-on: ubuntu-latest
    steps:
      - name: "My Step"
        run: echo "hello world $MY_ACTION"
        env:
          MY_ACTION: ${{ github.action }}

